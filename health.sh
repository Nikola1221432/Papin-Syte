node -e "const sqlite3 = require('sqlite3'); const db = new sqlite3.Database('./server/notes.db'); db.all('SELECT * FROM health_notes', [], (err, rows) => { if (err) console.error(err); else console.log(JSON.stringify(rows, null, 2)); }); db.close();" | grep name
